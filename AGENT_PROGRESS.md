# Agent Progress

- Completed: removed the remaining same-type warmup-solver argument conversions from `src/filter/inversion.jl` across the higher-order inversion likelihood and output-producing callsites. The boundary state vectors still convert once where the incoming storage can differ, and the `I_aug` / `I_state_vol` / `I_exo` casts remain in the generic `R <: Real` paths because those constants still need scalar-type adaptation.
- Validated: `get_errors` is clean on `src/filter/inversion.jl`, and a focused root-project `Gali_2015_chapter_3_nonlinear` probe now returns finite inversion loglikelihoods on dense and NaN-missing data plus finite `get_estimated_shocks` outputs across `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order` with `warmup_iterations = 2` after the redundant callsite conversions were removed.

- Completed: added a rerunnable identity-representation probe at `tasks/benchmark_warmup_identity_representations.jl` to compare the warmup-cache identities `I_aug`, `I_state_vol`, and `I_exo` as dense `Float64`, sparse `Float64`, and sparse `Int` on the actual warmup kernels where possible plus on generic identity-sensitive downstream expressions.
- Validated: `julia --startup-file=no --history-file=no --project=. tasks/benchmark_warmup_identity_representations.jl` now runs cleanly. On the SW07-sized pruned-second-order warmup kernel, sparse `Float64` identities were materially faster and lighter than dense `Float64` (`229.2 us`, `804.2 KiB` vs `494.7 us`, `5.6 MiB`), while the sparse-`Int` expression slice was effectively tied with sparse `Float64` (`105.4 us` vs `102.0 us`, same `286.8 KiB`). On the Gali-sized third-order warmup kernel, dense `Float64` was faster (`99.9 us` vs `123.6 us`) while sparse `Float64` used fewer bytes (`392.8 KiB` vs `614.1 KiB`); the sparse-`Int` slice again matched sparse `Float64` rather than beating it (`102.4 us` vs `104.4 us`, same `152.7 KiB`).
- Conclusion: there is still no blanket source refactor to sparse identities. The speed/alloc story is mixed across warmup orders, and sparse `Int` does not buy a clear runtime or allocation advantage over sparse `Float64` in the tested identity-sensitive expressions. Any future change would need to be selective by algorithm/path, and a true sparse-`Int` end-to-end experiment would also require widening the current `AbstractMatrix{Float64}` identity arguments in the warmup kernels and rrules.
- Completed: removed the redundant `convert(Vector{Float64}, ...)` and `convert_warmup_matrix(Float64, ...)` wrappers from the Float64-only third-order warmup callsites in `src/rrules.jl`; the generic `convert_warmup_matrix(::Type{R}, ...)` helper still remains on the primal side where `R <: Real` genuinely varies.
- Validated: a targeted `grep` on `src/rrules.jl` now finds no remaining Float64 warmup wrappers, `get_errors` stays clean on `src/rrules.jl`, and `julia --startup-file=no --history-file=no --project=. -e 'using MacroModelling; println("ok")'` still loads the package.
- Completed: added a shared `normalize_presample_periods` helper and threaded it through every presample-bearing public wrapper, primal likelihood kernel, matching `rrule`, the ForwardDiff Kalman specialization, and the plotting/test helpers; oversized `presample_periods` values are now clamped to the data length seen by each function instead of relying on downstream `max(..., 0)` guards or a plotting-side assertion.
- Updated: the public `get_loglikelihood` and plotting docstrings now state that `presample_periods` is clamped to the retained sample length with an informational message.
- Validated: `grep` finds no remaining `max(n_obs - presample_periods, 0)` clamps under `src/`; `get_errors` is clean on the touched source, extension, and test-helper files; `julia --startup-file=no --history-file=no --project=. -e 'using MacroModelling; println("ok")'` still loads; and a focused FS2000 probe confirms that `get_loglikelihood(...; presample_periods = 10000)` on a boundary-trimmed sample matches the explicitly clamped retained-sample value.
- Completed: rewrote the dense first-order inversion warmup pullback blocks in `src/rrules.jl` to use shared helpers built around `mul!`, `axpy!`, `copyto!`, and explicit dense row scatters, covering both the dense and missing-data warmup `rrule` paths without routing indexed row updates through copied submatrices.
- Validated: `get_errors` is clean on `src/rrules.jl`; `julia --startup-file=no --history-file=no --project=. -e 'using MacroModelling; println("ok")'` still loads the package; and direct `ChainRulesCore.rrule(MacroModelling.get_loglikelihood, ...)` probes on `Gali_2015_chapter_3_nonlinear` now return finite dense and missing-data `:first_order` inversion warmup likelihoods plus finite parameter cotangents with `warmup_iterations = 2`.
- Blocked externally: the existing focused warmup test file could not be run through the checked-in test harness because this workspace lacks a usable `test/Project.toml`/test-only dependency setup (`Preferences` in the wrapper path, `ForwardDiff` in direct file execution), so validation stayed on the direct root-project `ChainRulesCore` probes.

- Completed: evaluated whether the sparse auxiliary matrices in `Second_order_indices()` / `Third_order_indices()` should switch from `SparseMatrixCSC{Int}` to `SparseMatrixCSC{Bool}` and added a rerunnable probe at `tasks/benchmark_sparse_int_vs_bool.jl`; the only source edit in `src/options_and_caches.jl` was removing the unused `empty_sparse_bool` local after confirming no blanket type switch would be applied.
- Validated: the benchmark script shows the decision cannot be global. Some matrices are semantically non-binary and therefore cannot be stored as Bool without changing results (`σ_sym` has value `2`; third-order `𝐏` and `𝐏𝐂₃` contain `1`, `2`, and `3`). For binary matrices, the speed story is mixed: on SW07-sized second-order paths `dense * σ` and `∇₁₊ * Iₙ₊` were faster with `Int`, `U₂ * σ * C₂` was effectively tied, and `∇₂ * U∇₂'` was slightly faster and smaller with `Bool`; on Gali-sized third-order binary candidates the permutation multiplies were effectively tied and `dense * C₃` was modestly faster with `Bool`.
- Conclusion: no source-type refactor was applied yet because the hot-path results are mixed and the current constructor fields include both binary and non-binary matrices; any follow-up should be selective rather than a blanket `Int`→`Bool` replacement, and would also need to touch the empty-sentinel checks in `src/MacroModelling.jl` and `src/parser/model_setup.jl` for any fields whose type changes.
- Completed: the plot-only checks for an empty informative sample and an oversized `presample_periods` value now live in `trim_informative_sample` behind an explicit strict mode instead of being duplicated in `ext/StatsPlotsExt.jl`; the default helper path remains permissive so public APIs that intentionally return empty outputs on all-missing samples keep their existing behavior.
- Validated: `get_errors` is clean on `src/get_functions.jl` and `ext/StatsPlotsExt.jl`; a focused root-project Julia probe confirmed that `trim_informative_sample` still returns an empty retained sample by default on fully unobserved data and now throws the consolidated assertion in strict mode; and `grep` confirmed the old assertion messages no longer appear in `ext/StatsPlotsExt.jl`.
- Blocked externally: direct runtime execution of the `plot_model_estimates` extension methods could not be completed in the available environment because `using StatsPlots` fails with `Package StatsPlots not found in current path`, so the plotting callsites were validated statically plus through the shared helper behavior they now delegate to.
- Completed: the higher-order inversion likelihood and `rrule` paths no longer introduce a local `score_start` alias when it only equals `presample_periods`; the loops and effective-observation counts now read `presample_periods` directly in `src/filter/inversion.jl` and `src/rrules.jl`.
- Validated: after removing that alias, `grep` found no remaining `score_start` references under `src/`, `get_errors` stayed clean on `src/filter/inversion.jl` and `src/rrules.jl`, and the focused root-project Gali probe still returned finite dense and missing-data inversion loglikelihoods plus finite `rrule(get_loglikelihood, ...)` parameter cotangents across `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order` with `presample_periods = 2` and `warmup_iterations = 0` or `2`.
- Completed: repaired the temporary `src/rrules.jl` keyword-order regression introduced while threading warmup constants through the higher-order pullback callsites; the package loads again and the focused root-project Gali warmup/`rrule` probe is green across dense and missing-data `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order` cases.
- Completed: the newer higher-order inversion warmup code now follows the repo's storage split more closely: fixed warmup identity matrices live on `constants.second_order` / `constants.third_order`, while the touched primal likelihood, public output, and `rrule` warmup entry points reuse `workspaces.inversion` for mutable state/kron buffers instead of rebuilding them ad hoc.
- Validated: focused root-project Gali probes now return finite higher-order inversion loglikelihoods and finite `rrule(get_loglikelihood, ...)` parameter cotangents across dense and missing-data `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order` cases with `warmup_iterations = 2`; matching public `get_estimated_shocks` / `get_estimated_variables` probes also pass on dense and NaN-missing data.
- Confirmed: the public inversion output wrappers still accept missing observations through `Float64` arrays with `NaN` markers, not `Missing`-typed `KeyedArray`s; the broader missing-data support remains on the likelihood side.

- Completed: inversion-filter warmup now uses the hidden-only warmup shock prior interpretation across dense and missing-data likelihood kernels and rrules, while the first visible-period shock remains scored in the ordinary loop.
- Completed: `get_loglikelihood` now preserves inversion `warmup_iterations` instead of normalizing them away.
- Updated: `test/test_inversion_filter_gradients.jl` now checks the `warmup_iterations = 1` no-op invariant instead of warmup/presample equivalence.
- Confirmed: warmup and presample are intentionally different for inversion likelihoods; `warmup_iterations = 1` contributes zero hidden warmup prior terms and should match no warmup.
- Completed: a follow-up consistency pass removed dead inversion bookkeeping left behind by the warmup migration, including unused `precision_factor` locals, unused primal `warmup_jac` bindings, and one duplicate `n_hidden_warmup_shock_dims` assignment.
- Completed: higher-order inversion warmup is now implemented in the output-producing `filter_data_with_model` paths for dense and missing data across second-, pruned-second-, third-, and pruned-third-order algorithms using the same joint warmup solve as the likelihood paths.
- Completed: public higher-order warmup now flows through `get_estimated_shocks`, `get_estimated_variables`, and `get_shock_decomposition` after removing the temporary wrapper-side blocks.
- Validated: FS2000 dense and missing-data public output warmup now differs from prepended-data runs across second-, pruned-second-, third-, and pruned-third-order accessors, including pruned second- and third-order shock decompositions; this matches the intended joint hidden-warmup semantics.
- Completed: output-producing inversion paths now skip the joint warmup solve when `warmup_iterations = 1`, so the public output accessors respect the same hidden-only no-op semantics already used by the likelihood and `rrule` paths.
- Completed: pruned second- and third-order output decompositions now pre-advance their hidden warmup prefix in state space instead of materializing warmup decomposition columns that were immediately sliced away.
- Updated: the pruned Aumann-Shapley decomposition drivers now accept an optional hidden warmup shock prefix and advance their baseline and tangent recursions through that prefix before filling the visible sample.
- Updated: `test/test_missing_data.jl` now includes a focused regression that `warmup_iterations = 1` is a no-op for inversion output shocks, variables, and decompositions on dense and missing FS2000 data.
- Validated: `julia --project=. tasks/check_second_order_output_joint_warmup.jl` still confirms the higher-order output warmup behavior is distinct from prepended-data runs after the decomposition rewrite, and a direct root-project regression probe confirms the new `warmup_iterations = 1` output no-op invariant.
- Blocked externally: direct execution of `test/test_missing_data.jl` still fails before reaching the new regression because the available environment does not have `ADTypes`; this is separate from the warmup changes and the focused root-project probes still pass.
- Completed: removed the thin primal wrappers around `solve_second_order_joint_warmup_shocks_with_jacobian`, `solve_pruned_second_order_joint_warmup_shocks_with_jacobian`, and `solve_third_order_joint_warmup_shocks_with_jacobian`; the primal likelihood and output paths now call the jacobian-returning variants directly and ignore the unused middle return.
- Validated: a focused `julia --project=. -e ...` FS2000 probe still returns finite dense and missing-data inversion likelihoods, shocks, and variables for `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order` with `warmup_iterations = 2` after the direct-call refactor.
- Completed: public filtering wrappers and `get_loglikelihood` now trim leading and trailing fully unobserved periods before dispatching into Kalman or inversion kernels, with matching preprocessing in the `get_loglikelihood` rrule.
- Completed: the StatsPlots filtering/model-estimate extension now trims fully unobserved leading and trailing periods before calling `filter_data_with_model`, so it follows the same boundary contract as the public wrappers.
- Completed: after that caller-side alignment, the redundant empty-first-period warmup branches were removed from the first-, second-, pruned-second-, third-, and pruned-third-order inversion output/likelihood kernels and their matching missing-data `rrule` warmup paths.
- Completed: the first-order inversion output warmup path now reuses `solve_first_order_warmup_shocks` instead of materializing one-shot `svd` objects at the call site; the other explicit factorization sites were left unchanged because they either feed later `logabsdet`/inverse work or would change the solve algorithm if flattened to a bare `\`.
- Completed: the only remaining live `svd(...) \ rhs` call in `src/filter/inversion.jl` has been simplified to plain `\` in the first-order per-period missing-data output solve after confirming that Julia matches the SVD result on the relevant rectangular and rank-deficient `Float64` cases; the other `svd` hits in `src/` are comments only.
- Validated: a focused FS2000 public-API probe now matches trimmed-sample results for inversion loglikelihoods, shocks, variables, shock decompositions, Kalman standard deviations, and the `get_loglikelihood` pullback; the all-missing sample returns zero loglikelihood and empty outputs.
- Completed: added `tasks/higher_order_warmup_implementation.md` documenting the higher-order warmup design, including the joint warmup solve, the role of `jac_aug`, and the matching analytical rrules.
- Updated: `test/test_missing_data.jl` now guards the new higher-order output warmup semantics instead of the removed prepended-data equivalence.

## Joint warmup migration (current)

- Completed: analytic joint warmup (no finite differences) is implemented in inversion likelihood primal paths for dense and missing-data `:second_order` and `:pruned_second_order`.
- Completed: analytic joint warmup (no finite differences) is now implemented for the dense `:third_order` inversion likelihood path.
- Completed: analytic joint warmup (no finite differences) is now implemented for the missing-data `:third_order` inversion likelihood path using period-1 observed-row subsets.
- Completed: analytic joint warmup is now implemented for dense and missing-data `:pruned_third_order` inversion likelihood paths.
- Validated: `tasks/validate_third_order_joint_warmup.jl` returns finite values and confirms `warmup_iterations = 3` is distinct from `presample_periods = 3` on FS2000.
- Validated: `tasks/validate_missing_third_order_joint_warmup.jl` returns finite values and confirms missing-data warmup is distinct from both no-warmup and presample on FS2000.
- Validated: `tasks/validate_pruned_third_order_joint_warmup.jl` and `tasks/validate_missing_pruned_third_order_joint_warmup.jl` both return finite values and confirm warmup differs from presample on FS2000.
- Completed: rrule synchronization in `src/rrules.jl`; warmup-related inversion rrules now use analytical adjoints across second-, pruned-second-, third-, and pruned-third-order paths.
- Validated: focused FS2000 dense and missing-data checks now confirm `warmup_iterations = 1` matches `warmup_iterations = 0` in both the primal loglikelihood and the public pullback for `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order`.
- Validated: the same `warmup_iterations = 1` no-op invariant still holds after the dead-code cleanup on dense and missing Gali inversion paths across all five algorithms.
- Validated: `tasks/check_warmup_rrule_fd.jl` and `tasks/check_rrule_fd_nowarmup.jl` both report about `1e-6` or better on FS2000 with the 5-point FD stencil.
- Validated: `tasks/check_warmup_rrule_fd_model_sweep.jl` checks warmup lengths `0:10` on the small `FS2000` model (all five inversion algorithms, dense + missing) and the larger `Smets_Wouters_2007` model (`:first_order` and `:pruned_second_order`, dense + missing); all cases remain tightly matched to external FD.
- Validated: `tasks/check_second_order_output_joint_warmup.jl` now exercises the public higher-order output accessors on dense and missing FS2000 data and confirms the new joint warmup behavior is distinct from prepended-data runs.

## Filter-free warmup

- Completed: `get_filter_free_loglikelihood` now accepts `warmup_iterations` and expects `shocks` to include `n_data + max(warmup_iterations - 1, 0)` columns.
- Completed: filter-free warmup is implemented in the primal loglikelihood loops and analytical rrules for `:first_order`, `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order`.
- Completed: committed warmup gradient coverage was added to `test/test_filter_free_gradients.jl` by threading warmup kwargs through the shared closures and adding a warmup joint-gradient block.
- Validated: `tasks/tmp_filter_free_warmup_probe.jl` now returns a finite FS2000 filter-free likelihood for `warmup_iterations = 2`.
- Validated: `tasks/tmp_filter_free_warmup_fd_check.jl` matches a 5-point central FD gradient on `Gali_2015_chapter_3_nonlinear` across all five filter-free algorithms with `warmup_iterations = 3`; max abs error stayed at or below `4.6e-7`.
- Blocked externally: the supported `TEST_SET=gradient_checks` harness currently fails before running tests because `Pkg.test()` re-resolves into the existing `Pigeons`/`FlexiChains`/`DynamicPPL` compatibility conflict.

## ForwardDiff and filter-free boundary follow-up

- Completed: the public filter-free likelihood now trims fully unobserved leading and trailing periods through the shared helper, aligns explicit shock paths and matrix-valued measurement-error inputs to the retained sample, and warns up to three times so callers can realign externally supplied shocks.
- Completed: the public filter-free rrule now mirrors the trimmed forward path across all five algorithms, including observed-row subsets and cotangent expansion back to the user-supplied shock and measurement-error shapes.
- Completed: higher-order inversion warmup likelihood kernels no longer gate the joint-warmup solve on `R === Float64`; the dense and missing-data second-, pruned-second-, third-, and pruned-third-order paths now pass generic `R <: Real` inputs through the same warmup solver.
- Completed: `solve_second_order_joint_warmup_shocks`, `solve_pruned_second_order_joint_warmup_shocks`, and `solve_third_order_joint_warmup_shocks` are now generic in their scalar type instead of hard-coding `Float64` work arrays.
- Completed: higher-order warmup primal and `rrule` call sites now use a shared sparse-preserving element-type cast helper instead of forcing `Matrix{R}` / `Matrix{Float64}` on warmup operators that were already density-switched upstream.
- Updated: `test/test_filter_free_gradients.jl` now includes boundary-trimming regressions that compare the full boundary-missing sample against an explicitly trimmed reference and checks that the analytical pullback returns zero cotangents for discarded boundary shock columns.
- Updated: `test/test_inversion_filter_gradients.jl` now includes higher-order `warmup_iterations = 2` ForwardDiff/Zygote/FD cross-checks on dense and missing Gali data so the generic inversion warmup path is exercised directly.
- Updated: `test/functionality_tests.jl` now owns the legacy boundary-trimming regressions for `get_loglikelihood`, `get_filter_free_loglikelihood`, `get_estimated_shocks`, `get_estimated_variables`, and `get_shock_decomposition` across the legacy filter and algorithm matrix.
- Updated: `test/test_estimation.jl`, `test/test_1st_order_inversion_filter_estimation.jl`, `test/test_2nd_order_estimation.jl`, `test/test_pruned_2nd_order_estimation.jl`, `test/test_3rd_order_estimation.jl`, and `test/test_pruned_3rd_order_estimation.jl` no longer carry boundary-case coverage; those estimation scripts now stay focused on their estimation/NUTS/MAP paths.
- Updated: `test/test_helpers.jl` now provides reusable boundary-missing and filter-free shock-alignment helpers so legacy test files can express trimmed-vs-retained sample equivalence without depending on the new dedicated regression files.
- Updated: `src/common_docstrings.jl`, `src/get_functions.jl`, and `README.md` now describe inversion/filter-free warmup semantics, inversion ForwardDiff support, and the filter-free missing-data boundary trimming behavior.
- Updated: `README.md` now contains a filter-likelihood support matrix that explicitly separates primal support, analytical reverse-mode support, and committed-test coverage across Kalman, inversion, and filter-free likelihood variants, including the remaining coverage gaps.
- Updated: the `get_loglikelihood` and `get_filter_free_loglikelihood` docstrings now mirror that distinction instead of implying that every supported primal combination is equally covered by reverse-mode tests; the filter-free docstring also now lists `:first_order` among the supported algorithms.
- Completed: the recent filter-free `rrule` pullback helpers in `src/rrules.jl` now replace broadcast-heavy dense outer products, Kronecker adjoints, and cotangent copies with `mul!`, `axpy!`, `copyto!`, and `kron!` updates, while keeping only the genuinely scalar/scatter formulas elementwise.
- Completed: the recent warmup, trimming, and filter-free helper functions now keep their named function boundaries for reuse/readability, but no longer carry blanket `@inline` hints; the heavy helpers in `src/rrules.jl`, `src/get_functions.jl`, and `src/filter/inversion.jl` now rely on Julia's normal inlining heuristics instead of forcing hints on allocating/branching code.
- Completed: the older `src/`-level inline audit removed the remaining blanket `@inline` hints from `cache_valid_for_parameters`, the NSSS symbolic helpers, and the cache-growth/cache-set helpers; `_scatter!` in the ILU preconditioner was kept because it is still the one clearly tiny inner-loop helper in that set.
- Completed: helper names added or touched in this thread no longer start with `_`; the filter-free measurement-error helpers, workspace-growth helpers, the pruned-third-order Aumann–Shapley helper, and the sparse accumulator helper were renamed to descriptive public-style identifiers, and `AGENTS.md` now records that rule.
- Completed: the low-level `mul!`/`axpy!` filter-free pullback code now carries short comments explaining the in-place accumulation logic so the performance-oriented syntax stays readable.
- Completed: the one-off filter-free warmup-count wrappers were inlined back into the two call sites that used them, while `period_me_std` was kept as the shared scalar/vector/matrix measurement-error selector and reduced to the three methods that are actually used by the primal loops and the `rrule`.
- Completed: the higher-order inversion joint-warmup solvers now expose a slim primal API that returns only the solved warmup shocks plus a success flag; the Jacobian-returning variants remain available only for the analytical `rrule` path.
- Completed: the one-off inversion helper `hidden_warmup_periods` was inlined into `hidden_warmup_shock_dimension`, while the shared warmup shock-dimension and norm helpers stayed in place because both primal and `rrule` paths still use them.
- Validated: `tasks/reproduce_dual_and_filter_free_trim.jl` now reports matching full-vs-trimmed filter-free primals, matching filter-free rrule primals, and near-machine-precision agreement between analytical cotangents and manual FD checks for kept versus discarded shock columns.
- Validated: a direct kernel-level probe on `Gali_2015_chapter_3_nonlinear` now shows Float64 and BigFloat agreement at roughly `1e-15` across dense and missing inversion warmup likelihood kernels for `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order` after reusing the solved Float64 perturbation objects with structure-preserving casts.
- Validated: a focused `julia --project=.` probe now passes the relocated functionality-level filter-free boundary checks on FS2000 across `:first_order`, `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order`.
- Validated: `tasks/tmp_filter_free_warmup_fd_check.jl` still matches the analytical filter-free pullback to a 5-point FD gradient after the BLAS-style rewrite across `:first_order`, `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order`; max abs error stayed below `2.3e-7` with `warmup_iterations = 3`.
- Validated: after removing the recent blanket `@inline` annotations, `tasks/tmp_filter_free_warmup_fd_check.jl` still matches the analytical filter-free pullback across `:first_order`, `:second_order`, `:pruned_second_order`, `:third_order`, and `:pruned_third_order` (max abs diff stayed below about `4.6e-7`) and `tasks/validate_first_order_missing_warmup_rrule.jl` still matches the first-order inversion warmup adjoint (max `∂S` diff about `3.7e-9`, max `∂data` diff about `1.3e-9`).
- Validated: after the older `src/` inline audit, `get_errors` remained clean for `src/MacroModelling.jl`, `src/steady_state/nsss_solver.jl`, and `src/options_and_caches.jl`, and `MM_ALGO=second_order julia --project=. tasks/tmp_filter_free_warmup_fd_check.jl` still passed with max abs diff about `6.9e-8`, covering package load, NSSS setup, cache checks, and the filter-free path.
- Validated: after the underscore-name cleanup and guide update, `get_errors` is clean on the renamed files plus `AGENTS.md`; `MM_ALGO=second_order julia --project=. tasks/tmp_filter_free_warmup_fd_check.jl` still matches FD at about `2.3e-7`; `julia --project=. tasks/validate_first_order_missing_warmup_rrule.jl` still matches the first-order inversion warmup adjoint; and a focused `get_shock_decomposition(...; algorithm = :pruned_third_order, marginal_contribution = true)` probe on `RBC_baseline` returns the expected shock keys `[:ϵᵍ₍ₓ₎, :ϵᶻ₍ₓ₎, :Initial_values]`.
- Validated: after trimming the filter-free helper set, `get_errors` stays clean on `src/get_functions.jl` and `src/rrules.jl`; `MM_ALGO=second_order julia --project=. tasks/tmp_filter_free_warmup_fd_check.jl` still matches FD at about `2.3e-7`; and direct `:first_order` filter-free calls on `Gali_2015_chapter_3_nonlinear` still return finite values for both vector- and matrix-valued `measurement_error_std` inputs.
- Validated: after the higher-order warmup solver split, `get_errors` stays clean on `src/filter/inversion.jl` and `src/rrules.jl`; `julia --project=. tasks/validate_third_order_joint_warmup.jl` still returns finite third-order warmup values on FS2000; and `julia --project=. tasks/check_warmup_rrule_fd.jl` still matches the analytical warmup pullbacks to external FD across all five inversion algorithms on dense and missing data with max abs errors around `1e-6` or better.
- Validated: after the sparse-preserving warmup cast change, `get_errors` stays clean on `src/filter/inversion.jl` and `src/rrules.jl`; `julia --project=. tasks/check_second_order_output_joint_warmup.jl` still validates the higher-order output warmup behavior on dense and missing FS2000 data; and `julia --project=. tasks/check_warmup_rrule_fd.jl` still matches the analytical warmup pullbacks to external FD across all five inversion algorithms with max abs errors around `1e-6` or better.
- Validated: after trimming the remaining plotting callers and deleting the redundant empty-first-period warmup guards, `get_errors` is clean on `src/filter/inversion.jl`, `src/rrules.jl`, and `ext/StatsPlotsExt.jl`; `julia --startup-file=no --history-file=no --project=. -e 'using MacroModelling; println("ok")'` succeeds; and `julia --project=. tasks/check_second_order_output_joint_warmup.jl`, `julia --project=. tasks/validate_third_order_joint_warmup.jl`, and `julia --project=. tasks/check_warmup_rrule_fd.jl` all still pass.
- Validated: after the first-order warmup solve cleanup, `get_errors` remains clean on `src/filter/inversion.jl`, and a focused `julia --project=. -e ... get_estimated_shocks(FS2000, ...; algorithm = :first_order, filter = :inversion, warmup_iterations = 3)` probe returns finite dense and missing-data shock paths of the expected size.
- Validated: after simplifying the remaining live first-order missing-data `svd(...) \ rhs` call to plain `\`, `get_errors` stays clean on `src/filter/inversion.jl`; the focused FS2000 dense/missing `get_estimated_shocks(...; algorithm = :first_order, filter = :inversion, warmup_iterations = 3)` probe still returns finite outputs of size `(2, 192)`; and standalone Julia checks show `A \ b` matches `svd(A) \ b` on underdetermined, tall, and rank-deficient test systems.
- Blocked externally: direct execution of the ForwardDiff-based test files with `julia --project=. test/...` still fails in the root project because `ForwardDiff` is not a direct dependency of that environment, even though the library code and focused probes validate the new paths.
